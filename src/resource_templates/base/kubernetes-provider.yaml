name: default-kubernetes
description: The Default Kubernetes Provider
resource_type: Gestalt::Configuration::Provider::CaaS::Kubernetes
properties:
  config:
    env:
      public: {}
      private: {}
    networks: null
    storage_classes: []
    auth: {}
    external_protocol: https
  linked_providers: []
  environment_types: []
  # provider_subtype: Default  # Not present in schema prior to migrate
